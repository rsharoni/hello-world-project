apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 5000
      targetPort: 5000
      # nodePort: 30000 <-- let k8s choose the nodePort automatically
